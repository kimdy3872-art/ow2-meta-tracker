# OW2 메타 트래커 — 2차 다듬기 지시서

> 배포본(2026-08-17) 스크린샷 기준 진단 + 수정 지시.
> v1 문서의 일부 지침(`overflow:visible`, `translateY` 띄우기)이 겹침 버그의 원인이므로 **폐기**한다.

---

## PART A. 진단 — 왜 "AI티"가 나는가

### A-1. 목업의 배경을 UI로 옮겨왔다

참고한 Valorant 대시보드 이미지의 **보라색 그라디언트는 Dribbble 샷의 촬영 배경**이지 제품 화면이 아니다. 지금 앱은 그 보라 배경 위에 화면이 떠 있는 형태다. 실제 제품은 배경까지 화면이 꽉 찬다.
→ **보라 배경 제거. `--bg:#0a0c12` 단색으로 화면 전체를 채운다.**

### A-2. 모든 카드가 같은 무게

현재 전 카드에 동일한 `radius 24px` + `0 20px 45px rgba(0,0,0,.5)` 그림자가 걸려 있다. 그림자 blur 45px이 20개 쌓이면 화면이 뿌옇고, 무엇이 중요한지 알 수 없다. 이게 "생성형 대시보드" 인상의 최대 원인이다.

→ **그림자는 HERO 카드 1개에만. 나머지는 `1px solid var(--line)` 만.**
→ **라운드에 위계: HERO 20px / 일반 카드 12px / 칩·배지 8px / 표 행 0px.**

### A-3. 이모지 4종

| 위치 | 현재 | 교체 |
|---|---|---|
| "🏆 영웅 랭크 순위표" | 트로피 이모지 | **삭제.** 제목만 |
| MINOR/MAJOR PERKS "👍 58%" | 따봉 이모지 | 가로 프로그레스 바 |
| HEROES 카드 "♡" | 하트 이모지 | 인라인 SVG (stroke 1.5) |
| 시계열 "▲ 4.7%" | 텍스트 화살표 | CSS 삼각형 또는 SVG |

참고 목업의 이모지 개수는 0개다.

### A-4. 제목에 걸린 glow가 글자를 뭉갠다

"오버워치 2 경쟁전 메타 센터", "영웅 랭크 순위표", "분석 조건" 등에서 글자가 번져 보인다. Black Han Sans처럼 획이 굵은 폰트에 `text-shadow` blur를 넣으면 속공간이 메워져 가독성이 떨어진다.
→ **제목의 text-shadow / glow 전부 제거. 단색 `var(--txt)`.**

### A-5. 같은 말을 두 번 한다

- 헤더 "티어와 포지션별 지표를 대시보드 형식으로 확인할 수 있습니다."
- 바로 아래 "분석 조건 — 티어, 포지션, 정렬 기준을 먼저 고르면 아래 랭킹과 요약이 즉시 갱신됩니다."

둘 다 화면을 보면 아는 내용이다. UI 설명문은 사용자가 헤맬 때만 필요하다.
→ **"분석 조건" 카드 통째로 삭제.** 3D·시계열·상세 페이지의 동일 패턴도 전부 삭제.

### A-6. 드롭다운 4개

참고 목업에 드롭다운은 0개다. 특히 **티어는 브론즈~그랜드마스터 순서가 있는 값**이라 드롭다운에 넣으면 (a) 현재 위치가 안 보이고 (b) 매번 열어야 하고 (c) 화면이 "설문 폼"처럼 보인다.

| 필터 | 현재 | 변경 |
|---|---|---|
| 티어 | selectbox | 가로 세그먼트 7칸 |
| 포지션 | selectbox | 아이콘 토글 4칸 (전체/공격/돌격/지원) |
| 정렬 | selectbox | **삭제** → 표 헤더 클릭 정렬 |
| 영웅 검색 | text_input | 유지 (돋보기 아이콘 인라인) |
| 밴률/승률/픽률 라디오 | radio | **삭제** → TOP4 카드 자체의 탭으로 흡수 |

드롭다운 4개 + 라디오 1개 → **컨트롤 3개**로 줄어든다.

---

## PART B. 겹침 버그 — 근본 원인과 절대 수칙

### 원인

Streamlit은 모든 블록을 세로 flex 컨테이너에 넣고 z-index를 관리하지 않는다. 여기서 `position:absolute`나 음수 `transform`을 쓰면 문서 흐름에서 빠져나가 stacking context가 예측 불가능해진다. 현재 확인된 증상:

- 우측 레일(META SCORE / 최근 변동 / 밴률 TOP 3 / 랭크 분포)이 본문 위로 떠서 HERO 카드와 순위표를 가림
- 순위표가 컨테이너 오른쪽을 넘어가 "종합 점수" 열이 잘림
- 영웅 상세에서 초상화가 흰 박스째 "전장별 승률" 제목을 덮음
- HERO 카드 자리에 검은 빈 공간 (아트 부재 + absolute 실패)

### 절대 수칙 4가지

```
1. position:absolute 는 "카드 내부에서 부모가 position:relative + overflow:hidden 인 경우"에만 허용.
   카드 경계를 넘어가는 absolute는 전면 금지.

2. 음수 margin, 음수 translateY 로 요소를 띄우지 않는다. (TOP MAPS 강조 카드 포함)

3. 우측 레일은 반드시 st.columns 의 실제 컬럼이어야 한다.
   고정하고 싶으면 position:sticky; top:1rem 만 사용. absolute/fixed 금지.

4. 모든 카드 컨테이너에 min-width:0 을 준다.
   (flex 자식의 기본 min-width:auto 때문에 표가 컬럼을 뚫고 나간다)
```

### HERO 카드 아트 처리 — 방식 변경

기존: `<img>` 를 카드 밖으로 삐져나오게 (실패)
변경: **아트를 카드 배경으로 넣고 `overflow:hidden`**

```css
.hero-card{
  position:relative; overflow:hidden;      /* ← visible 아님 */
  min-height:260px; border-radius:20px;
  background:
    linear-gradient(100deg, rgba(10,12,18,.96) 0%, rgba(10,12,18,.75) 42%, transparent 78%),
    url("data:image/png;base64,...") right center / auto 118% no-repeat,
    linear-gradient(115deg,#7a1030,#3b1a5c);
}
```

삐져나오는 효과는 포기하고, **좌측 그라디언트 페이드로 아트가 배경에 녹아드는** 방식으로 간다. 겹침이 원천 차단되고, 아트 해상도가 낮아도 덜 티가 난다.

---

## PART C. 삭제 · 이동 · 추가 결정표 (페이지 1)

### 현재 순서의 문제

```
헤더 → 분석조건 안내 → 필터 → 라디오 → 밴률TOP4 → 랭크분포
→ [영웅 랭크 순위표] 제목 → FAQ 아코디언 2개 → HERO카드 → HEROES
→ 최근변동 → TOP MAPS → 실제 표
```

**표 제목과 표 사이에 무관한 블록 5개가 끼어 있다.** 이게 "배치가 부자연스럽다"고 느낀 지점이다. 제목 바로 아래에는 그 제목의 내용물이 와야 한다.

### 결정

| 블록 | 결정 | 이유 |
|---|---|---|
| 보라 배경 | **삭제** | 목업의 촬영 배경 |
| "분석 조건" 안내 카드 | **삭제** | 화면 보면 아는 내용 |
| 밴률/승률/픽률 라디오 | **삭제** | TOP4 카드 탭으로 흡수 |
| 정렬 드롭다운 | **삭제** | 표 헤더 클릭으로 대체 |
| 🏆 이모지 | **삭제** | — |
| FAQ 아코디언 2개 | **최하단 이동** | 참조용. 첫 화면 자리 아님 |
| 랭크 분포 | **우측 레일 이동** | 지금 본문 위에 떠서 표를 가림 |
| 최근 변동 / META SCORE / 밴률 TOP3 | **우측 레일에 정렬** | 이미 의도한 위치. absolute만 제거 |
| 패치노트 | **복구 후 최하단** | 현재 화면에서 사라졌음 |
| 표본 수(경기 수) | **추가** | 픽률 3.3% 솜브라와 58.6% 키리코가 같은 신뢰도로 보임 |
| 마지막 갱신 시각 | **추가** | 사이드바 "DATA 2026-08-17"을 상단 라이브 점으로 |

### 새 순서

```
[상단 고정바]  로고 없음(사이드바에 있음) / 티어 세그먼트 / 포지션 토글 / 검색
─────────────────────────────────────────────
[본문 좌 3.4]                        [우측 레일 1.5, sticky]
  HERO 카드 (1위 영웅, 260px)          META SCORE
  밴률 TOP 4 (탭: 밴률/승률/픽률)       최근 변동 4행
  TOP MAPS 4장                        밴률 TOP 3
  ── 영웅 랭크 순위표 ──               랭크 분포
  표 (전체 폭)
─────────────────────────────────────────────
[최하단 전체 폭]
  패치노트 (expander, 접힘)
  랭크는 어떻게 산정되나요? (expander)
  메타 유형 라벨은 뭔가요? (expander)
```

HEROES 가로 스크롤 카드는 **삭제 권장**. 순위표 상위 6명과 정보가 100% 중복이고, 아트가 없어서 지금 빈 카드로 보인다. 아트를 확보한 뒤 다시 넣는다.

---

## PART D. 복붙용 지시문

### D-1. 전역 정리

```
전역 CSS와 레이아웃을 다음 기준으로 정리해줘.

[삭제]
- body/.stApp 의 보라색 그라디언트 배경 전부 제거. --bg:#0a0c12 단색으로 채워줘.
- 모든 제목의 text-shadow / glow / filter:blur 제거. 단색 var(--txt).
- 카드 그림자를 HERO 카드 1개만 남기고 전부 제거.
  나머지 카드는 border:1px solid var(--line); background:var(--card); 만.
- 이모지 전부 제거: 🏆(순위표 제목), 👍(퍽), ♡(즐겨찾기), ▲▼(델타).
  ♡와 ▲▼는 인라인 SVG로 교체, 👍는 가로 프로그레스 바로 교체.
- 각 페이지의 "분석 조건 / 분포 조건 / 시계열 조건 / 상세 조건" 안내 카드 4개 전부 삭제.
- 헤더 아래 한 줄 설명문도 삭제 (헤더 제목만 남김).

[라운드 위계]
--r-hero:20px; --r-card:12px; --r-chip:8px;
표 행은 라운드 0.

[겹침 방지 — 필수]
- position:absolute 를 쓰는 모든 요소를 찾아서, 부모가
  position:relative + overflow:hidden 인 경우가 아니면 전부 제거해줘.
- 음수 margin / 음수 translateY 전부 제거. (TOP MAPS 강조 카드 포함)
- 모든 컬럼 컨테이너와 표 래퍼에 min-width:0 추가.
- 우측 레일은 st.columns 의 실제 컬럼으로 만들고,
  고정은 position:sticky; top:1rem 만 사용. fixed/absolute 금지.

[여백]
[data-testid="stVerticalBlock"]{gap:14px}
.block-container{padding-top:1rem; max-width:1440px}
섹션 사이 32px, 카드 내부 padding 18px.
```

### D-2. 필터바 교체

```
페이지 1의 필터 영역을 다음으로 교체해줘.

[티어] selectbox → 가로 세그먼트 컨트롤
  브론즈 실버 골드 플래티넘 다이아 마스터 그랜드마스터
  st.segmented_control 사용. 없는 버전이면 st.radio(..., horizontal=True) +
  CSS로 라디오 동그라미 숨기고 pill 버튼처럼.
  활성 항목: background var(--accent); color #fff; 비활성: transparent + border.

[포지션] selectbox → 4칸 아이콘 토글 (전체 / 공격 / 돌격 / 지원)
  아이콘은 Lucide SVG 인라인 (crosshair / shield / heart-pulse).

[정렬] selectbox 삭제.
  대신 순위표 헤더(승률/픽률/밴률/종합 점수)를 클릭 가능하게 만들고,
  현재 정렬 열에 ▾ SVG 표시 + var(--accent) 색.
  정렬 상태는 st.session_state["sort_by"], ["sort_desc"] 로 관리.

[검색] 유지. 돋보기 SVG를 인풋 왼쪽 안쪽에 넣고 label 숨김.

[밴률/승률/픽률 라디오] 삭제.
  대신 "밴률 TOP 4" 카드 제목 우측에 작은 탭 3개(밴률·승률·픽률)를 붙여서
  그 카드 안에서만 전환되게 해줘.

이 4개를 sticky 상단바 한 줄로 묶어줘 (position:sticky; top:0; z-index:10;
background:var(--bg); 하단 border 1px).
```

### D-3. 순위표

```
순위표가 컨테이너 오른쪽을 넘어가서 "종합 점수" 열이 잘리고 있어. 다음으로 고쳐줘.

[열 축소] 현재 7열 → 5열
  1) 영웅 (초상화 36px + 이름 + 그 아래 작은 글씨로 "공격 · 밴 압박")
     → 포지션 열과 메타 라벨 배지를 이 셀에 흡수. 열 2개가 줄어든다.
  2) 승률  3) 픽률  4) 밴률  5) 종합 점수 (+랭크 뱃지)

[폭] grid-template-columns: minmax(200px,1.6fr) repeat(3, minmax(110px,1fr)) 130px;
     래퍼에 overflow-x:auto; min-width:0;

[표본] 승률 값 옆에 회색 작은 글씨로 표본 경기 수 표시.
  픽률 3.3%인 솜브라와 58.6%인 키리코가 지금 같은 신뢰도로 보여서 오해를 준다.

[헤더] "PORTRAIT" 라벨 삭제(빈 열). 헤더는 sticky top:0 으로 스크롤 시 고정.
[행] 높이 56px, 홀짝 배경 없음, hover 시에만 background rgba(255,255,255,.03).
[막대] 높이 4px, radius 2px. 현재보다 얇게. 값 텍스트는 막대 오른쪽 고정 폭 정렬.
[nowrap] 모든 셀 white-space:nowrap.
```

### D-4. HERO 카드

```
HERO 카드가 지금 검은 빈 공간으로 나온다. 다음으로 재작성해줘.

- 아트를 <img>가 아니라 카드 background 레이어로 넣고 overflow:hidden.
  background: linear-gradient(100deg, rgba(10,12,18,.96) 0%,
              rgba(10,12,18,.75) 42%, transparent 78%),
              url(base64) right center / auto 118% no-repeat,
              linear-gradient(115deg, {hero_color_1}, {hero_color_2});
- 아트 파일이 없으면 배경 그라디언트 + 큰 워터마크 숫자만으로 성립하게
  폴백을 만들어줘. 검은 빈 칸이 나오면 안 된다.
- 워터마크 숫자(밴률)는 opacity .12, 카드 안쪽에만 (overflow:hidden으로 잘림 허용).
- 좌측: eyebrow "종합 점수 1위" → 영웅명 56px → 
  "골드 · 공격 · 랭크 S" 한 줄 → 스탯 4칸 그리드.
- 숫자와 단위가 줄바꿈되는 문제: "51.4 %" 처럼 떨어지고 있어.
  <span class="num">51.4</span><span class="unit">%</span> 로 묶고
  부모에 white-space:nowrap, .unit{font-size:.45em; margin-left:2px}.
```

### D-5. 3D 분포 페이지

```
- 2D와 3D를 나란히 두니 3D가 잘린다. st.columns([1.15, 1]) 로 2D를 넓히고
  3D 높이를 460px로 고정, scene.aspectmode="cube".
- 3D의 "영웅 랭크" 범례 박스가 데이터를 가린다.
  범례를 차트 밖으로 빼서 legend=dict(orientation="h", y=-0.12) 로 하단 배치.
  또는 2D/3D 공통 범례 하나만 남기고 3D 범례는 showlegend=False.
- 3D 축 라벨이 회전되어 읽기 어렵다. scene.xaxis.title 등에
  font size 11, 축 tick은 nticks=5로 줄여줘.
- 하단 설명문 3줄이 너무 길다. 
  "색상=랭크 · 점 크기=밴률 · 라벨은 상위 8명" 한 줄로 줄이고 나머지는 삭제.
- 메타 유형 칩 5개는 잘 되어 있다. 다만 현재 어떤 칩도 선택 안 된 상태와
  전체 선택 상태가 구분이 안 되니, 선택 시 채움/미선택 시 outline으로 명확히.
```

### D-6. 시계열 페이지

```
이 페이지가 가장 완성도가 높다. 미세 조정만.
- 델타 배지 ▼0.6% 의 텍스트 화살표를 CSS 삼각형 또는 SVG로 교체.
- 51.4% 의 % 크기를 0.45em으로 줄이고 baseline 맞춰줘 (지금 따로 논다).
- 기간 슬라이더가 단일 핸들인데 "2026-05-18 ~ 2026-08-17" 구간을 보여주고 있다.
  st.slider 에 value=(start, end) 튜플을 넘겨 range 슬라이더로 바꿔줘.
- 영웅 아트 카드(좌측 세로)와 KPI 3개의 높이가 안 맞는다. 
  같은 st.columns 안에서 align-items:stretch 로 높이 통일.
```

### D-7. 영웅 상세 페이지

```
겹침이 가장 심한 페이지다.
- 상단 HERO 카드 자리가 큰 검은 공백이다. D-4의 폴백 규칙을 적용해줘.
- 프레야 초상화가 흰 박스째 "전장별 승률" 제목을 덮고 있다.
  이 초상화의 position:absolute 를 제거하고 좌측 컬럼 안에 정상 배치.
  흰 배경은 이미지 자체 여백이니 카드에 overflow:hidden + object-fit:cover.
- 좌측 "프레야 / 공격 / 하위 역할·수색가" 블록이 HERO 카드와 정보 중복이다. 삭제.
- MINOR PERKS / MAJOR PERKS: 아이콘 64px, 이름을 아이콘 아래 한 줄 nowrap,
  선호도는 👍 대신 가로 바 + 우측 퍼센트. 선호도 높은 쪽만 var(--pos) 색.
- "MAJOR PERKS"가 두 줄로 깨진다. nowrap 적용.
- TOP WIN / TOP PICK 배지가 전장 이름과 겹친다.
  행을 position:relative 로 잡고 배지를 우상단 absolute (부모 내부이므로 허용),
  전장 이름에 padding-right 확보.
```

---

## PART E. 아직 남은 근본 과제

| 항목 | 상태 | 영향 |
|---|---|---|
| **영웅 전신 아트** | 없음 | HERO 카드·HEROES 섹션이 빈 칸. **이게 목업과의 가장 큰 격차** |
| 전장 썸네일 | 없음 | TOP MAPS가 색 블록뿐 |
| 영웅 대표색 JSON | 없음 | 모든 HERO 카드가 같은 보라/빨강 |
| 표본 수 컬럼 | 없음 | 신뢰도 왜곡 |
| 랭크 뱃지 SVG | 텍스트 | S/A/B/C/D가 그냥 글자 |

아트 없이 CSS만 다듬으면 여기서 더 좋아지기 어렵다. **PART D를 적용한 뒤에는 아트 확보가 다음 단계의 전부**라고 봐도 된다.

---

## PART F. 적용 순서

1. **D-1 전역 정리** — 보라 배경·그림자·이모지·안내문 제거. 여기서 체감이 가장 크다.
2. **PART B 겹침 수칙** — absolute/음수 margin 일괄 제거. 버그 해소.
3. **PART C 재배치** — FAQ·패치노트 최하단, 랭크 분포 레일로.
4. **D-2 필터바** — 드롭다운 → 세그먼트.
5. **D-3 순위표** — 열 축소, 잘림 해소.
6. D-4~D-7 개별 페이지.
7. 아트 확보 후 HERO 카드 재작업.

1~3번만 해도 "AI티"의 대부분이 사라진다.
