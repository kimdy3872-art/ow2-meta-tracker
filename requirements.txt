# data-testid 셀렉터에 의존하는 CSS 가 있어 버전을 고정한다.
# 올릴 때는 assets/style.css 의 [data-testid=...] 규칙이 여전히 맞는지 확인할 것.
streamlit==1.50.0
plotly>=5.17.0
pandas>=2.0.0
numpy>=1.24.0
selenium>=4.10.0
webdriver-manager>=4.0.0
pyarrow>=16.0.0
